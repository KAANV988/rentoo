package com.rentoo.rentooapp.model;

/**
 * Defines the user roles within the application.
 * A user can either be an OWNER (listing properties) or a RENTER (looking for properties).
 */
public enum Role {
    OWNER,
    RENTER
}